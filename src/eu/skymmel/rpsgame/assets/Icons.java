package eu.skymmel.rpsgame.assets;

import static eu.skymmel.rpsgame.assets.Colors.*;

public final class Icons {
    public static final String warn = yellow + "[!] " + reset;
    public static final String question = yellow + "[?] " + reset;
    public static final String join = green + "[+] " + reset;
    public static final String line = blue + "---------------------------" + reset;
}
